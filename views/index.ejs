<!DOCTYPE html> <!-- Telling the browser what type of document will be shown-->
<html lang="en"><!--Starting html tag and  Saying that language is English-->
<head><!-- starting head tag to store our meta data-->
    <meta charset="UTF-8"><!--indicates what chars set should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"><!-- sets appropriate compatibility for Microsoft browsers-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"><!-- Sets the WIDTH of available viewing area to the width of the device screen-->
    <title>Document</title><!--Assign a title that apears on the browser tab-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"><!--creates a link to the font awesome icon library-->
    <link rel="stylesheet" href="css/style.css"><!--Creates a link to the local stylesheet in the public folder-->
</head>
<body> <!-- main section for content displayed -->
    <h1>Todo List: </h1> <!-- Main title displayed on page-->
    <ul class="todoItems"> <!-- Create unordered list and assign class to the list-->
    <% for(let i=0; i < items.length; i++) {%> <!-- Use EJS to start for loop-->
        <li class="item"> <!--Create list item with a class of item-->
            <% if(items[i].completed === true) {%> <!-- Use EJS to check if item in an array with completed prop is true-->
                <span class='completed'><%= items[i].thing %></span> <!-- Creating an inline element with a class and adding EJS to display the name of the task -->
            <% }else{ %> <!-- EJS for the Else clause of out if statemnet-->
                <span><%= items[i].thing %></span> <!-- adding EJS to display the name of the task with no CLASS -->
            <% } %> <!-- EJS to close else statement-->
            <span class='fa fa-trash'></span> <!-- Display font awesome icon of Trashcan-->
        </li><!--closing our list item-->
    <% } %> <!-- EJS to close for loop-->
    </ul> <!--Closing our unoredered list-->

    <h2>Left to do: <%= left %></h2> <!-- create a second level heading tag with text and using EJS to display a variable showing how many tasks is left to complete -->

    <h2>Add A Todo:</h2> <!--Create a second level header-->

    <form action="/addTodo" method="POST"> <!-- Create a form with POST method that goes to the /addTodo route -->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!--Creating input field, accepts text, default text before you type anything and set name of input box-->
        <input type="submit"> <!-- Gives the form a Submit button-->
    </form><!-- Close the form tag-->
    

    <script src='js/main.js'></script> <!-- Links to the main JS file located in the js folder in the public folder -->
</body>
</html>
